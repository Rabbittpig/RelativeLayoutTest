<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="10dp"
    tools:context=".RelativeLayoutActivity">

    <EditText
        android:id="@+id/et_line"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="InputText" />
<!--  android:layout_alignParentRight="true"等同于 android:layout_alignRight="@id/et_line"-->
    <Button
        android:id="@+id/btn_cancel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/et_line"
        android:layout_alignRight="@id/et_line"
        android:text="取消" />
<!--   android:layout_below="@id/et_line"等同于 android:layout_alignTop="@id/btn_cancel"-->
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@id/btn_cancel"
        android:layout_toLeftOf="@id/btn_cancel"
        android:text="确定" />

</RelativeLayout>